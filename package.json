{
  "name": "digital-web-2023",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "yarn build && firebase deploy --only=hosting"
  },
  "devDependencies": {
    "vite": "^5.1.6"
  }
}
